# QUOTE-WEB-SCRAPER
A python web scraper that extracts quotes, authors, and tags from websites. Features automatic pagination, multiple export formats (CSV/JSON), error handling, and data statistics. Built with BeautifulSoup and Requests.
